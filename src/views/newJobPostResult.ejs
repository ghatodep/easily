<div class="container bg-dark text-light rounded m-4 p-3">
  <h1 class="modal-title fs-3 text-center">Create a New Job Opening Post</h1>
  <div class="fs-5 fw-semibold text-center m-4">
    <% if(jobDetails!=null){ %>
    <div class="alert alert-success" role="alert">
      Job details have been successfully added !<br />
      A new job opening post for the role of
      <span class="text-warning"><%= locals.jobDetails.designation %></span>
      at
      <span class="text-primary"><%= locals.jobDetails.compName %></span>
      has been created.
    </div>
    <%}else{%>
    <div class="alert alert-danger m-4" role="alert">
      Sorry! There was a issue during the process please apply again after some
      time.
    </div>
    <%}%>
  </div>
  <div class="d-flex justify-content-center">
    <a class="btn btn-primary btn-lg" href="/home">Redirect to Home Page</a>
  </div>
</div>
